# interceptions

Count of `event.type = 'INTERCEPTION'` attributed to the player — StatsBomb
logs this as its own event type, so no outcome filtering is needed.

See [foundation_adjustments.md](foundation_adjustments.md) for `per_90`,
`possession`, and `opposition_strength`.
