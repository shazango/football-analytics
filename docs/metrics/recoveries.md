# recoveries

Count of `event.type = 'RECOVERY'` attributed to the player — StatsBomb's
dedicated ball-recovery event, its own event type, no outcome filtering
needed.

See [foundation_adjustments.md](foundation_adjustments.md) for `per_90`,
`possession`, and `opposition_strength`.
